# FastAPI 核心
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
chromadb==1.5.9

# 文件上传处理（UploadFile + Form 必须）
python-multipart==0.0.9

# 文档解析
PyMuPDF==1.24.10
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==1.0.2

# 文本切分（LangChain 轻量包，不引入完整 langchain）
langchain-text-splitters==0.3.8

# 结构化日志（可选；未安装时自动回退到 text 格式）
python-json-logger==2.0.7
